/* Mes heures v23: notice seuils et delais */
(function(){
function enrichHelp(){const view=document.querySelector('#settingsView');if(!view||document.querySelector('#helpBalanceDeadlineV23'))return;const intro=view.querySelector('.card .muted');if(intro&&intro.textContent.includes('cimetière des trucs utiles mais flemme'))intro.innerHTML='Le guide grandit avec l’application. Parce qu’une fonction sans explication finit tôt ou tard dans le <em>« cimetière des trucs utiles mais flemme »</em>.';const cards=[...view.querySelectorAll('.card')],solde=cards.find(c=>c.querySelector('h2')?.textContent.includes('Solde'));if(solde){const block=document.createElement('div');block.id='helpBalanceDeadlineV23';block.innerHTML='<p>Tu peux définir une limite basse, une limite haute et un délai maximum pour revenir dans cette zone. Le délai commence au premier franchissement observé et ne redémarre pas tant que le solde reste hors des clous.</p><p>Le délai peut être saisi en jours, semaines ou mois. Si la fin de contrat arrive avant l’échéance calculée, c’est elle qui devient prioritaire. Sans délai personnalisé, la fin de contrat sert seule d’échéance.</p><p>L’effort conseillé porte uniquement sur ce qu’il faut corriger pour revenir au seuil, pas forcément jusqu’à zéro.</p><p class="muted">L’appli prévient avant que le compteur ne parte faire sa vie tout seul.</p>';solde.appendChild(block)}}
const oldOpen=window.openHelpV21;window.openHelpV21=()=>{oldOpen();enrichHelp()};
})();